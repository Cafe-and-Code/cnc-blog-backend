name: Blog.API.Testing

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Apply appsettings
      run: |
        Copy-Item -Path "C:/secret_config/Blog.API/appsettings.json" -Destination "./Blog.API/appsettings.json"
        Remove-Item -Path "./Blog.API/appsettings.Development.json" -Force
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Restore dependencies
      run: |
        cd Blog.API
        dotnet restore
    - name: Build
      run: |
        cd Blog.API
        dotnet build --configuration Release
        dotnet publish -c release -o ./publish
    - name: Test
      run: |
        cd Blog.API
        dotnet test --no-build --verbosity normal

  migrate:
    runs-on: self-hosted
    needs: build
    steps:
      - name: Read connection string
        id: get-connection-string
        run: |
          $appSettings = Get-Content -Raw -Path "./Blog.API/appsettings.json" | ConvertFrom-Json
          $connectionString = $appSettings.ConnectionStrings.DefaultConnection
          $escapedString = $connectionString -replace '"','\"'
          echo "connection_string=$escapedString" >> $env:GITHUB_ENV
        shell: powershell
    
      - name: Install EF Tools
        run: |
          dotnet tool install --global dotnet-ef
        shell: powershell
      
      - name: Migrate
        run: |
          cd Blog.API
          dotnet ef database update --connection "${{ env.connection_string }}"
        shell: powershell

  deploy:
    runs-on: self-hosted
    needs: [build, migrate]
    steps: 
      - name: Stop IIS
        run: |
          echo "Stopping IIS Server..."
          iisreset /stop   
      - name: Kill processes
        run: |
          echo "Stopping processes that might be using the files..."
          handle.exe C:\inetpub\wwwroot\apitesting.cafencode.ddns.net | ForEach-Object {
            if ($_ -match 'pid: (\d+)') {
              $pid = $matches[1]
              taskkill /PID $pid /F
            }
          }
      - name: Deploy
        run: |
          cd Blog.API
          echo "Copying files to the web root..."
          robocopy ./publish "C:\inetpub\wwwroot\apitesting.cafencode.ddns.net" /E /Z
          $robocopyExitCode = $LASTEXITCODE
          if ($robocopyExitCode -ge 8) { echo "File copy failed with exit code $robocopyExitCode"; exit $robocopyExitCode }
          echo "Complete Copy files to the web root."
          exit 0
      - name: Start IIS
        run: |
          echo "Starting IIS Server..."
          iisreset /start
          exit 0